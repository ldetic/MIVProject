@model IEnumerable<MIVProject.supplierCategory>
@using Resources
@{
    ViewBag.Title = Local.SupplierCategory;
}

<h2>@Local.SupplierCategory</h2>

<p>
    @Html.ActionLink(Local.CreateNew, "Create")
</p>
<table id="supplierCategory-table" class="table object-table" data-toggle="table" data-sort-name="id" data-sort-order="asc"
       data-search="true" data-toolbar="#toolbar" data-pagination="true"
       data-query-params="queryParams">
    <thead>
        <tr>
            <th>
                @Local.Name
            </th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.ActionLink(Local.Edit, "Edit", new { id = item.id }) |
                @Html.ActionLink(Local.Delete, "Delete", new { id = item.id })
            </td>
        </tr>
    }

</table>
