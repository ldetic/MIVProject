@model MIVProject.userType
@using Resources
@{
    ViewBag.Title = @Local.Details;
}

<h2>@Local.Details</h2>

<div>
    <h4>@Local.UserType</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.type)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink(Local.Edit, "Edit", new { id = Model.typeID }) |
    @Html.ActionLink(Local.Back, "Index")
</p>
