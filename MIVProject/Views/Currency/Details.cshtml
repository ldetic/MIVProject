@model MIVProject.currency
@using Resources
@{
    ViewBag.Title = @Local.Details;
}

<h2>@Local.Details</h2>

<div>
    <h4>@Local.Currency</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.abbreviation)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.abbreviation)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink(Local.Edit, "Edit", new { id = Model.currencyID }) |
    @Html.ActionLink(Local.Back, "Index")
</p>
