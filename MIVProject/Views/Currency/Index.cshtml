@model IEnumerable<MIVProject.currency>
@using Resources
@{
    ViewBag.Title = Local.Currency;
}

<h2>@Local.Currency</h2>

<p>
    @Html.ActionLink(Local.CreateNew, "Create")
</p>
<table id="currency-table" class="table object-table" data-toggle="table" data-sort-name="id" data-sort-order="asc"
       data-search="false" data-toolbar="#toolbar" data-pagination="true"
       data-query-params="queryParams">
    <thead>
        <tr>
            <th data-sortable="true">
                @Local.Name
            </th>
            <th>
                @Local.Abbreviation
            </th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.abbreviation)
            </td>
            <td>
                @Html.ActionLink(Local.Edit, "Edit", new { id = item.currencyID }) |
                @Html.ActionLink(Local.Delete, "Delete", new { id = item.currencyID })
            </td>
        </tr>
    }

</table>
