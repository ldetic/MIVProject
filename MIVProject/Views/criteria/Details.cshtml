@model MIVProject.criteria
@using Resources
@{
    ViewBag.Title = @Local.Details;
}

<h2>@Local.Details</h2>

<div>
    <h4>@Local.Criteria</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Local.Name
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink(@Local.Edit, "Edit", new { id = Model.criteriaID }) |
    @Html.ActionLink(@Local.Back, "Index")
</p>
