@model IEnumerable<MIVProject.supplyStatus>
@using Resources
@{
    ViewBag.Title = Local.SupplyStatus;
}

<h2>@Local.SupplyStatus</h2>

<p>
    @Html.ActionLink(Local.CreateNew, "Create")
</p>
<table id="supplyStatus-table" class="table object-table" data-toggle="table" data-sort-name="id" data-sort-order="asc"
       data-search="false" data-toolbar="#toolbar" data-pagination="true"
       data-query-params="queryParams">
    <thead>
    <tr>
        <th data-sortable="true">
            @Local.Name
        </th>
        <th></th>
    </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.ActionLink(Local.Edit, "Edit", new { id = item.statusID }) |
                @Html.ActionLink(Local.Delete, "Delete", new { id = item.statusID })
            </td>
        </tr>
    }

</table>
